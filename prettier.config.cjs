// Базовая конфигурация Prettier для синхронизации форматирования
module.exports = {
  // Максимальная длина строки; 80 символов удобно при ревью и сравнениях.
  printWidth: 80,
  // Отступ в 2 пробела (унифицировано для целого репозитория).
  tabWidth: 2,
  // Жёстко используем пробелы, чтобы избежать разночтений с табуляцией.
  useTabs: false,
  // Всегда ставим точку с запятой (возвращает привычный стиль JS).
  semi: true,
  // Предпочитаем двойные кавычки (соответствует большинству исходников).
  singleQuote: false,
  // Добавляем запятую в конце списков/объектов (упрощает диффы).
  trailingComma: "all",
  // Записываем пробелы внутри фигурных скобок (`{ foo: bar }`).
  bracketSpacing: true,
  // Всегда оборачиваем параметры стрелочных функций в скобки.
  arrowParens: "always",
  // Единый перевод строки — LF, чтобы не плодить mix LF/CRLF.
  endOfLine: "lf",
};

