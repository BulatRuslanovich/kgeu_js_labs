let obj = {
    '1x': 1,      // Обязательно: начинается с цифры
    'y2': 2,      // Не обязательно: начинается с буквы, содержит только буквы и цифры
    'z-z': 3,     // Обязательно: содержит дефис (спецсимвол)
    'd 4': 4,     // Обязательно: содержит пробел
}
